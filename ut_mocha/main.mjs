function capitalize(word) {
    return word.toUpperCase();
}

function isEvenNumber(number) {
    return number % 2 === 0;
}

function cloneArray(arr) {
    return [...arr];
}

export { capitalize, isEvenNumber, cloneArray };
